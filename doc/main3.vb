'On sait que l'eau gêle à zéro dégré ; l'eau salé gêle à moins 3 degré 
'le fiol a -5 dégré , l'ordilaire gële à -13 dégré , le super gêle à -23 dégrés 
' ecris un programme qui demande une température et donne sort la liste des liquides qui gêles à cette température 

Module 
  Sub Main()
      dim temp as integer
      console.WriteLine("entrer la température: ")
      temp = console.Readline()
    
      if temperature >= 0 then
        Console.ReadLine("L'eau gèle à cette température.") 
      if temperature >= -3 then
        Console.ReadLine("L'eau salée gèle à cette température.")
      if temperature >= -5 then
        Console.ReadLine("Le fioul gèle à cette température.")
      if temperature >= -13 then
        Console.ReadLine("L'ordinaire gèle à cette température.")
      if temperature >= -23 then
        Console.ReadLine("Le super gèle à cette température.")
    End Sub 
end Module 

